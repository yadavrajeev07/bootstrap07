function addNumbers(a, b) {
    return a + b;
  }
  
  // Using the function
  let num1 = 5;
  let num2 = 10;
  let result = addNumbers(num1, num2);
  
  // Output the result
  console.log("The sum is: " + result);